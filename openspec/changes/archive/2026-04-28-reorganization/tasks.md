## 1. Project Spec Reorganization

- [x] 1.1 Update the long-lived `project-structure` spec so it focuses on physical project organization and package/layout rules only.
- [x] 1.2 Add the new long-lived `project-conventions` spec with the initial coding conventions baseline.
- [x] 1.3 Add the new long-lived `project-architecture` spec with the initial MVC and boundary guidance baseline.

## 2. Capability Alignment

- [x] 2.1 Move or restate project-wide guidance so architectural rules no longer live only in `project-structure`.
- [x] 2.2 Ensure coding-convention requirements live under `project-conventions` rather than being mixed into structural capabilities.
- [x] 2.3 Review the resulting project-wide specs to confirm each requirement belongs to the correct capability.

## 3. Verification

- [x] 3.1 Verify the new and modified spec files are complete, use valid requirement/scenario formatting, and are ready for future apply/archive workflows.
- [x] 3.2 Confirm the `reorganization` change artifacts fully describe the agreed flat capability naming approach: `project-structure`, `project-conventions`, and `project-architecture`.
