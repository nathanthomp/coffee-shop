## 1. Maven Foundation

- [x] 1.1 Create the Maven project descriptor for Java 21 with JavaFX and JUnit 5 dependencies.
- [x] 1.2 Configure Maven so the project supports standard compile, test, and JavaFX run workflows.
- [x] 1.3 Add a `.gitignore` for Maven build output and common IDE-generated files.

## 2. Source Structure

- [x] 2.1 Create the `src/main/java` and `src/test/java` source layout.
- [x] 2.2 Establish the `coffeeshop.*` package root and place MVC-related application classes under `coffeeshop.app`.
- [x] 2.3 Add the initial application bootstrap classes needed for a minimal MVC-friendly skeleton.

## 3. Runnable Skeleton

- [x] 3.1 Implement a pure JavaFX application entry point that launches successfully through the supported Maven workflow.
- [x] 3.2 Add a minimal initial window or scene that proves the JavaFX application is running without introducing feature-specific order behavior.
- [x] 3.3 Keep the startup structure ready for future separation of UI and non-UI responsibilities.

## 4. Test Baseline

- [x] 4.1 Add at least one JUnit 5 test that runs through Maven successfully.
- [x] 4.2 Verify the initial setup compiles and tests cleanly using the Maven workflows defined for the project.
